import styled from 'styled-components';
import { AppColors } from '../../constants/app_colors';

export const Footer = styled.footer`
  width: 100%;
  height: 30px;
  background-color: ${AppColors.footer};
`;
